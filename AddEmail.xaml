<Window x:Class="CloudScan.AddEmail"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="200"
        Height="200" Width="200"
	Title="AddEmail">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="10,50,10,49">
            <Label>Email Address:</Label>
            <TextBox Height="20" Name="emailTextBox"/>
        </StackPanel>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Click="Add_Button_Click">Add </Button>
            <Button Grid.Column="1" Click="Cancel_Button_Click">Cancel</Button>
        </Grid>
    </Grid>
</Window>
